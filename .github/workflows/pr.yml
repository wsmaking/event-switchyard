name: pr
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  gate0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
      - name: Gate0 (check-lite)
        run: make check-lite
